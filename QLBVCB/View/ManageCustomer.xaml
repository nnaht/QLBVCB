<UserControl x:Class="QLBVCB.View.ManageCustomer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLBVCB.View"
             mc:Ignorable="d" 
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"

             d:DesignHeight="740" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Left">
            <TextBox Text=""
                     x:Name="txtBoxSearch" 
                     Margin="55, 20, 0, 15"
                     Width="300"
                     Height="40"
                     FontSize="12"
                     BorderBrush="Black"
                     CaretBrush="Black"
                     materialDesign:HintAssist.IsFloating="False"
                     materialDesign:HintAssist.Background="Transparent"
                     materialDesign:HintAssist.Foreground="Black"
                     materialDesign:HintAssist.Hint="🔎 Tìm kiếm loại vé"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}">
                <TextBox.Padding>
                    <Thickness Left="5" Right="5" Top="10" Bottom="10"/>
                </TextBox.Padding>
            </TextBox>
        </Grid>
        <Grid Grid.Row="1" Height="585">
            <Grid Margin="5"
                  HorizontalAlignment="Center"
                  Background="White"
                  Width="760" Height="585">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                    <DataGrid ItemsSource="{Binding CustomerList}"
                              AutoGenerateColumns="False"
                              Background="#FFF8F8F8"
                              d:ItemsSource="{d:SampleData}"
                              SelectedItem="{Binding SelectedItem}"
                              Width="760" Height="570">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="Background" Value="#FFD90429"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="1,0,0,0"/>
                                <Setter Property="BorderBrush" Value="White"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="*" Header="Mã khách hàng" Binding="{Binding MAKH}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Họ tên" Binding="{Binding HOTEN}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Ngày sinh" Binding="{Binding NGAYSINH}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Giới tính" Binding="{Binding GIOITINH}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Căn cước công dân" Binding="{Binding CCCD}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Địa chỉ" Binding="{Binding DIACHI}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Số điện thoại" Binding="{Binding SDT}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Email" Binding="{Binding EMAIL}"></DataGridTextColumn>
                            <DataGridTextColumn Width="*" Header="Hoạt động" Binding="{Binding HOATDONG}"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Grid>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" HorizontalAlignment="Left" >
                <Button Height="40"
                        Margin="20, 0, 0, 0"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedLightButton}"
                        ToolTip="MaterialDesignRaisedLightButton with Round Corners" Background="#FFD90429" BorderBrush="#FFB11409" Width="140">
                    <TextBlock Text="Xuất File Excel" 
                               Foreground="#DDFEFEFE" />
                </Button>
            </Grid>
            <Button Grid.Column="1"
                    Style="{StaticResource MaterialDesignFloatingActionButton}"
                    Margin="10, 10, 20, 10"
                    ToolTip="Edit"
                    Background="#FFD90429"
                    HorizontalAlignment="Right"
                    Command="{Binding OpenAERCustomerCommand}">
                <materialDesign:PackIcon Kind="Edit" Height="24" Width="24" />
            </Button>
        </Grid>
    </Grid>
</UserControl>
